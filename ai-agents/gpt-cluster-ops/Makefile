.PHONY: setup run verify clean

setup:
	@echo "Setting up GPT Cluster Ops example..."
	@echo "Prerequisites: kranix-mcp (HTTP), OpenAI API, kranix-api, Kubernetes"

run:
	@echo "Starting kranix-mcp with HTTP transport..."
	# Add MCP server startup commands here

verify:
	@echo "Verifying HTTP transport..."
	# Add verification commands here

clean:
	@echo "Cleaning up resources..."
	# Add cleanup commands here
