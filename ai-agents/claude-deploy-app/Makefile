.PHONY: setup run verify clean

setup:
	@echo "Setting up Claude Deploy App example..."
	@echo "Prerequisites: kranix-mcp, Claude Desktop, kranix-api, Kubernetes"

run:
	@echo "Starting kranix-mcp server..."
	# Add MCP server startup commands here

verify:
	@echo "Verifying MCP connection..."
	# Add verification commands here

clean:
	@echo "Cleaning up resources..."
	# Add cleanup commands here
